name: CI/CD Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      TFSTATE_BUCKET: ${{ secrets.TFSTATE_BUCKET }}
      TFSTATE_KEY: ${{ secrets.TFSTATE_KEY }}

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repo
        uses: actions/checkout@v3

      # 2️⃣ Set up Terraform
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.8.5

      # 3️⃣ Get EC2 public IP from tfstate in S3
      - name: Get EC2 IP from tfstate in S3
        id: ec2ip
        run: |
          aws s3 cp s3://$TFSTATE_BUCKET/$TFSTATE_KEY terraform.tfstate --region $AWS_REGION
          IP=$(jq -r '.resources[] 
              | select(.type=="aws_instance" and .name=="nginx_server") 
              | .instances[0].attributes.public_ip' terraform.tfstate)
          IP=$(echo "$IP" | tr -d '\r\n ')
          if [[ -z "$IP" ]]; then
            echo "❌ No se pudo obtener la IP de EC2 desde tfstate"
            exit 1
          fi
          echo "EC2_IP=$IP" >> $GITHUB_ENV
          echo "✅ EC2 IP: $IP"

      # 4️⃣ Debug EC2 IP
      - name: Debug EC2 IP
        run: echo ">$EC2_IP<"

      # 5️⃣ Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13.7'

      # 6️⃣ Generate index.html
      - name: Generate index.html
        working-directory: monitoring-project/docker/nginx
        run: python generate_index.py

      # 7️⃣ Log in DockerHub
      - name: Log in DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      # 8️⃣ Build & push NGINX
      - name: Build & push NGINX
        working-directory: monitoring-project/docker/nginx
        run: |
          IMAGE_NAME=${{ env.DOCKERHUB_USERNAME }}/custom-nginx
          docker build -t $IMAGE_NAME:latest .
          docker push $IMAGE_NAME:latest

      # 9️⃣ Build & push Flask app
      - name: Build & push Flask app
        working-directory: monitoring-project/app
        run: |
          IMAGE_NAME=${{ env.DOCKERHUB_USERNAME }}/flask-app
          docker build -t $IMAGE_NAME:latest .
          docker push $IMAGE_NAME:latest

      # 🔟 Retrieve SSH key from SSM and save as file
      - name: Retrieve SSH key from SSM
        run: |
          aws ssm get-parameter \
            --name "/ssh/monitoring-project" \
            --with-decryption \
            --query "Parameter.Value" \
            --output text \
            --region $AWS_REGION > monitoring-project.pem
          chmod 600 monitoring-project.pem

      # 1️⃣1️⃣ Deploy stack on EC2
      - name: Deploy stack on EC2
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ env.EC2_IP }}
          username: ec2-user
          key_path: monitoring-project.pem
          script: |
            # Forzar PATH
            export PATH=$PATH:/usr/local/bin:/usr/bin

            # Crear carpeta principal si no existe
            if [ ! -d /opt/monitoring/monitoring-project ]; then
              sudo mkdir -p /opt/monitoring/monitoring-project
              sudo chown -R ec2-user:ec2-user /opt/monitoring
            fi
            cd /opt/monitoring/monitoring-project

            # Configurar git safe directory
            git config --global --add safe.directory /opt/monitoring

            # Si el repo no existe, clonarlo; si ya existe, limpiar y actualizar
            if [ ! -d .git ]; then
              git clone https://github.com/Rivce06/DevOps_Project_Portfolio.git .
            else
              git fetch --all
              git reset --hard origin/main
            fi

            # Verificar que los archivos para montar existen
            REQUIRED_FILES=("monitoring/prometheus.yml" "monitoring/grafana.yml")
            for f in "${REQUIRED_FILES[@]}"; do
              if [ ! -f "$f" ]; then
                echo "❌ File $f does not exist!"
                exit 1
              fi
            done

            # Levantar Docker Compose
            docker-compose pull
            docker-compose up -d --remove-orphans
