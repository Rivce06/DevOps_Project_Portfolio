events {}

http {
  server {
    listen 80;

    location / {
      root /usr/share/nginx/html;
      index index.html;
    }

    # Endpoint para métricas (Prometheus puede scrapear aquí)
    location /metrics {
      stub_status;
      allow all;
    }
  }
}
